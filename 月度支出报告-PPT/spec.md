# 2026年8月支出报告 — PPT 规格说明

演示文稿源：`月度支出报告-PPT.univer`（Slide 单元「2026年8月支出报告」），导出 `月度支出报告.pptx`。
数据与《月度支出表》《月度支出报告》一致：10 笔明细、6 个分类、合计 ¥1,131.80。

## 画布与页面

- 页面尺寸 960 × 540 px，白色背景。
- 每页成品交付 5 页；SVG 源文件保留在 `月度支出报告-PPT/page-*.svg`。

## 设计常量（deck-level，一次定稿）

- 主色（深蓝）`#1F4E79`；辅助蓝 `#4472C4`；浅蓝 `#D9E2F3` / `#DEEBF7`；
- 卡片底 `#F7F9FC`；分隔/细线 `#D9E2F3`；正文灰 `#333333`；次要灰 `#595959`；弱灰 `#808080`；
- 强调橙 `#ED7D31`；合计强调底 `#FFF2CC`；
- 分类色（按金额降序）：购物 `#ED7D31`、餐饮 `#FFC000`、水电燃气 `#4472C4`、娱乐 `#70AD47`、交通 `#5B9BD5`、医疗 `#C0392B`；
- 字体：CJK 用系统无衬线（PingFang SC / 微软雅黑），西文与数字随同；
- 字号（SVG px）：封面标题 44、章节标题 26、卡片数值 30、正文 13–15、辅助说明 11–12、页脚 9；
- 章节页统一页眉：标题基线 y≈56，标题下橙色短条（x 84, y 66, 宽 54, 高 5）；
- 章节页统一页脚：hairline y=516，左「2026年8月支出报告 · 数据源自《月度支出表》」，右「N / 5」；
- 不使用滤镜、透明渐变、非方形径向渐变；不用 `dx`；间距用显式坐标。

## 数据事实（全部数字来自前两版产物，可核对）

- 明细 10 笔（日期 分类 金额）：08-01 餐饮 86.50；08-02 交通 35.00；08-05 购物 259.00；08-08 餐饮 68.00；08-10 娱乐 88.00；08-15 水电燃气 156.40；08-18 交通 42.00；08-22 餐饮 122.00；08-25 医疗 75.00；08-28 购物 199.90。
- 合计 ¥1,131.80；记录 10 笔；分类 6 个；单笔平均 ¥113.18；统计期 28 天，日均 ¥40.4。
- 分类汇总（降序 + 占比）：购物 ¥458.90（40.5%）；餐饮 ¥276.50（24.4%）；水电燃气 ¥156.40（13.8%）；娱乐 ¥88.00（7.8%）；交通 ¥77.00（6.8%）；医疗 ¥75.00（6.6%）。
- 购物 + 餐饮 = ¥735.40（65.0%）；交通 + 娱乐 + 医疗 = ¥240.00（21.2%）。

## 逐页结构

### 页 1 封面（hero）
- 顶部横条：y 0–8 全宽 `#4472C4`。
- 左列文案区 x=80：
  - kicker（13px，`#4472C4`，加粗）：MONTHLY EXPENSE REPORT · AUGUST 2026
  - 标题（44px，加粗，`#1F4E79`）：2026年8月支出报告
  - 橙色短条（x 84, y 232, 宽 72, 高 6, rx 3, `#ED7D31`）
  - 副题（16px，`#595959`）：统计期间 2026-08-01 至 2026-08-28 · 记录 10 笔 · 覆盖 6 个支出分类
- 右下装饰：两个同心浅色圆（`#DEEBF7` 大圆 r≈150 在右上区、`#FFE699` 小圆 r≈90 在其右下），不与文字重叠。
- 底部 3 张统计卡（x 80/354/628，宽 250，高 104，圆角 8，底 `#F7F9FC`、边 `#D9E2F3`）：
  - 总支出 / ¥1,131.80
  - 记录笔数 / 10 笔
  - 日均支出（28 天口径）/ ¥40.4
- 页脚左小字（9px，`#808080`）：数据来源：《月度支出表》与《月度支出报告》。

### 页 2 支出总览（KPI 2×2）
- 标题：支出总览；右上说明：金额单位：人民币元。
- 4 张卡（x 80 与 x 492，宽 388，高 168；行 y 120 与 y 308；圆角 10；底 `#F7F9FC`、边 `#D9E2F3`）：
  1. 2026年8月总支出 / ¥1,131.80（数值 `#1F4E79`）
  2. 记录笔数 / 10 笔 / 副行：单笔平均 ¥113.18
  3. 日均支出 / ¥40.4 / 副行：按 28 天统计口径
  4. 最大支出分类 / 购物 / 副行：¥458.90 · 占比 40.5%（数值 `#ED7D31`）

### 页 3 支出明细（表格）
- 标题：支出明细；右上说明：金额单位：人民币元。
- 表区 x=80，宽 800；三列：日期 160（x 80–240，居中）、分类 240（x 240–480，左对齐）、金额（元）400（x 480–880，右对齐，文本锚 end 于 x=852）。
- 表头行高 40（y 118–158，底 `#4472C4`，白字 13px）；数据行 10 × 高 31（偶数行底 `#F7F9FC`，行间细线 `#E6EDF7`）；合计行高 40（y 468–508，底 `#FFF2CC`，行顶 2px `#1F4E79` 粗线）。
- 合计行：第 1 列「合计」（加粗 `#1F4E79`），第 2 列空，第 3 列 ¥1,131.80（加粗 `#1F4E79`）。

### 页 4 分类支出占比（横向条形）
- 标题：分类支出占比；右上说明：合计 ¥1,131.80。
- 6 行（起点 y=136，行距 54）按金额降序；行内：
  - 色块 14×14（x=88，圆角 3）+ 分类名（x=112，14px，`#333333`）+ 金额（x=112，11px，`#808080`）两行布局；
  - 条区轨道（x 330–850，高 22，圆角 11，底 `#F1F5FB`）；数值条同几何，圆角 11，填分类色；
  - 条宽 = 占比 / 40.5% × 520：购物 520、餐饮 313、水电燃气 177、娱乐 100、交通 87、医疗 85；
  - 条右外侧百分比标签（12px，加粗，`#1F4E79`，文本锚 start）。
- 底部说明（11px，`#808080`）：占比 = 分类金额 ÷ 合计金额，四舍五入到 0.1 个百分点。

### 页 5 结论与建议（双栏）
- 标题：结论与建议。
- 左栏卡（x 80–460，宽 380；顶 `#4472C4` 短条标题「结论」）：
  - 购物 + 餐饮合计 ¥735.40，占比 65.0%（大头支出）
  - 水电燃气 ¥156.40（13.8%），固定支出稳定可预期
  - 交通、娱乐、医疗合计 ¥240.00（21.2%），单笔金额较小
- 右栏卡（x 480–880，宽 400；标题「下月行动建议」，蓝色序号圆点）：
  1. 购物类设定 ¥400 预算上限，临近上限暂停非必要采购
  2. 餐饮 3 笔合计 ¥276.50，提高在家做饭比例
  3. 小额支出逐笔记账，月底复盘娱乐与交通频次
- 卡内正文 13–14px，`#333333`；行距 ≥ 34px 保证不重叠。
