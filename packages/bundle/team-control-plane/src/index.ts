/**
 * @deepseek-ai/dsh-team-control-plane — the Team Edition Control Plane as a
 * standalone profile bundle. The package's substance is `cordis.patch.yml`,
 * declared by the `dsh.bundle.patch` manifest field and resolved by the
 * profile composer through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-team-control-plane
 */

export {}
