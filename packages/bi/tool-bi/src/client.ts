/**
 * Client-namespace projection of the BI scope: a pure re-export of the
 * package's types outlet, so browser code names one client entry and reads
 * the very declaration the Host registers.
 * @module @deepseek-ai/dsh-tool-bi/client
 */

export type * from './types.ts'
